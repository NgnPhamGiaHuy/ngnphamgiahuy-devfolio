/**
 * The signature reveal/morph easing — single source of truth for JS motion.
 * CSS mirror: `--ease-standard` in styles/components/career.css.
 */
export const EASE_STANDARD = [0.22, 1, 0.36, 1] as const;
